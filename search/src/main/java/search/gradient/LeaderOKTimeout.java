package search.gradient;

import se.sics.kompics.timer.ScheduleTimeout;
import se.sics.kompics.timer.Timeout;

/**
 *
 * @author teo
 */
public class LeaderOKTimeout extends Timeout {

    public LeaderOKTimeout(ScheduleTimeout request) {
        super(request);
    }
}
